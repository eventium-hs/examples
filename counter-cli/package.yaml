name: examples-counter-cli
version: "0.3.2"
maintainer: Alexander Sidorenko
synopsis: Example Counter cli application for eventium
description: |
  A simple counter application demonstrating basic event sourcing concepts with Eventium. This minimal
  example is perfect for understanding the fundamentals of event sourcing, including command handling,
  event storage, and aggregate state reconstruction. It uses in-memory storage for simplicity and serves
  as an entry point for learning Eventium.
stability: experimental
license: MIT

flags:
  ci:
    description: Enable -Werror for CI builds
    manual: true
    default: false

when:
  - condition: flag(ci)
    ghc-options: -Werror

dependencies:
  - base >= 4.9 && < 5
  - eventium-core >= 0.6.0 && < 0.7.0
  - eventium-memory >= 0.6.0 && < 0.7.0

  - safe >= 0.3 && < 0.4
  - stm >= 2.5 && < 2.6

executables:
  counter-cli:
    main: counter-cli.hs
    ghc-options: -Wall -Wcompat -Widentities -Wincomplete-record-updates -Wredundant-constraints -Wno-type-defaults -Wno-incomplete-uni-patterns
    default-extensions:
      - NoFieldSelectors
      - DuplicateRecordFields
      - OverloadedRecordDot
